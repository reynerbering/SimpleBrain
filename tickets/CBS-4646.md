---
type: ticket
ticket: CBS-4646
team: CBS
status: Backlog
source: jira
repos:
  - CoreAPI
updated: 2026-09-18
---

# CBS-4646 — Core: Add site delete requirement and product duration check on stop posting/job process

- **Status:** Backlog (jira) — Story · High · reporter Shiela Mojeno · **unassigned** · label `CBSWk38of2026`
- **Design:** [[wiki/CBS-4646-site-delete-requirement-check.md]]
- **Last touched:** 2026-09-18

## 2026-09-18

**Did**

- Pulled the ticket from Jira via the Atlassian MCP connector. Title, status, description, comments and links verified — not reconstructed from `/raw`.
- Ran **Phase 1 grilling to completion**. Twelve questions asked and answered; a shared understanding was reached on all of them.
- Read the V2 stop-posting path and the V1 stop flows in `CoreAPI` to ground the interview in facts rather than the ticket's summary.
- Filed the design doc: [[wiki/CBS-4646-site-delete-requirement-check.md]] (`status: decided`), commit `991bd1e`, amended the same day with the verification SQL.
- Attempted to close the four open questions against prod MSSQL. **Could not — see Blockers.** Fell back to the DataGrip offline DDL cache, which confirmed neither V1 stop proc has the mechanism and narrowed OQ1 to "certainly a string, exact type still unknown".

**Decisions**

- Decisions live in the design doc, not here. Link, do not duplicate.
- Twelve decisions (D1–D12) reached. Phase 1 is **complete**.

**Context the Jira ticket does not carry** — found during recon, worth having at the top of a cold restart:

- **DBA-2636** (same reporter, *Deployed to Production*) already shipped this rule for `usp_oneclick_delete_nonintegrated_posting`. CBS-4646 is its Core V2 counterpart. DBA-2640 is the QA twin.
- **CBS-4646 blocks [PJO-11160](https://jobtarget.atlassian.net/browse/PJO-11160)** (SEEK site 25461, natural 30-day expiration, Resolved). PJO-11160 is where the *duration* half of the rule comes from and is far more specific than CBS-4646's own description.
- **CBS-4656** was the discovery ticket, reassigned to me 2026-09-14 and marked Complete. My comment on CBS-4646, 2026-09-16: *"This is ready to implement, we don't have this mechanism in Core V2."*
- Volume from Shiela, 2026-09-08: 43 postings across 9 sites, started ~Sept 2024. Table is in the design doc.

**Touched**

- `CoreAPI` — **read only, nothing modified.** Branch as found: `CBS-4643-ofccp-getjobs-param-validation`, HEAD `fcf24ca5`. No CBS-4646 branch cut yet.
  - ⚠️ That HEAD **predates the .NET 10 upgrade** (`ee28457e`, CBS-4668, 2026-09-10). The code was read pre-upgrade; nothing quoted in the design doc is affected, but **Phase 2 must branch from current `develop`**, not from here.
- Vault: `wiki/CBS-4646-site-delete-requirement-check.md` (new), this file.

**Pipeline**

- Verdict: **not run** — by design. Phase 1 finished today; Phase 2 is gated on the four open questions below.
- `CoreAPI` test command is pinned in [[protocols/coding-standards.md]]: `dotnet test CoreAPI.Test/CoreAPI.Test.csproj`. **Baseline is currently red** — 84 failed / 299 passed / 383 total, from container schema drift, not code.

**Blockers / Open questions**

- [ ] **NEXT — reconnect the VPN, start a *new* session, then run the four verification queries** in [[wiki/CBS-4646-site-delete-requirement-check.md#how-to-close-them]]. The SQL is written out there, ready to paste. MCP servers only attempt connection at session startup, so retrying inside a running session does nothing.
- **All three MSSQL MCP servers failed to connect 2026-09-17 and again 2026-09-18** — `qa-64recs-mssql`, `uat-64recs-mssql`, `prod-64recs-mssql`, all `CONNECT_TIMEOUT` at 30s. So did `jira`, `gitlab`, `datadog` and all three Postgres servers, while the cloud-routed Atlassian connector worked. That split points at the VPN, not the hosts. A connection failure, not a missing capability.
- The DataGrip cached-DDL fallback **half helped** — routine DDL is there in full (used it to confirm neither stop proc has the mechanism, and to narrow OQ1), per-column table DDL is not. Details and the working zip path are in the design doc under *How to close them*. Cache snapshot is **2026-07-08** and provably behind prod, so treat proc findings as dated.
- The four gates, in the order they matter:
  - [ ] **OQ1 — type of `job_distribution_transmit_site_setting.value`.** D7 assumes `string` compared `== "1"`. A wrong guess makes the gate never match, so *every* site reads as "does not require deletion" — the dangerous direction.
  - [ ] **OQ2 — is `requires_pending_delete = 1` populated for integrated sites?** This is D8's gate. DBA-2636's precedent covers non-integrated sites only. A large `unflagged` count stops the design.
  - [ ] **OQ3 — does the seeded `coreapi-test-sql` container have the settings table?** If not, the entity fails at query translation. **Where the fix goes changed on 2026-09-18:** add it to `CoreAPI.Test/Scripts/000_schema_drift.sql`, which is applied before every run. Not the integration-test schema scripts — those belong to `tests/CoreAPI.Tests.Integration`, which builds its own schema and is unaffected.
  - [ ] **OQ4 — which of the five write paths each of Shiela's nine sites takes.** Not a blocker (D5 gates all five) — it is how we confirm the fix landed.
- [x] **Before `/ship`: fix the test container and get a green baseline** (D12). **Done 2026-09-18 while working CBS-4643** — `CoreAPI.Test` is **383 / 383**, `tests/CoreAPI.Tests.Integration` **950 passed / 3 skipped**. The drift is now recorded in `CoreAPI.Test/Scripts/000_schema_drift.sql` and applied automatically before each run, so the hand-run `ALTER` is no longer needed — but that fix sits on the **unmerged** CBS-4643 branch (`55b6aa90`), so until it reaches `develop` a fresh container still needs `ALTER TABLE [64recs67o].dbo.jobs_info_long ADD site_id INT NULL;`. See [[wiki/CBS-4646-site-delete-requirement-check.md#2026-09-18--revision-pre-implementation]] and [[tickets/CBS-4643.md]].
- **Out of scope, deliberately, with nothing raised by us:**
  - The **V1 / stored-proc stop flows** (`usp_oneclick_posting_stop`, `usp_oneclick_job_stop`). Core V2 has no job-level stop at all, so the "job process" half of the ticket title cannot be built in this repo. Needs a DBA sibling ticket — **not raised.**
  - **Backfill of the 43 existing rows.** Neru: *"let them deal with that."*
- The ticket is still **unassigned** in Jira and sits in **Backlog** despite Phase 1 being done. Not changed by me — no Jira writes were made this session.
