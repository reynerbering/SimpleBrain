---
type: ticket
ticket: CBS-4436
team: CBS
status: Selected for Development
source: jira
repos:
  - CoreAPI
updated: 2026-09-17
---

# CBS-4436 — Discovery: analytics click-to-apply hash missing for postings created via core-api V2 create path

- **Status:** Selected for Development (jira) — Investigation · High · reporter Shervin Ivari · assignee me
- **Design:** [[wiki/CBS-4436-v2-posting-analytics-wrap.md]]
- **Last touched:** 2026-09-17

## 2026-09-17

**Did**

- Pulled the ticket from Jira via the Atlassian MCP connector. Title, status, description verified — not reconstructed from `/raw`.
- Started Phase 1 grilling. **Paused after Q1 was asked; Q1 is unanswered and no decision was reached.**
- Read the V1 and V2 posting-create paths in `CoreAPI` to ground the interview in facts rather than the ticket's summary.
- Filed the design doc: [[wiki/CBS-4436-v2-posting-analytics-wrap.md]] (`status: in progress`). It carries the verified facts and the full Q1–Q9 decision tree.

**Decisions**

- Decisions live in the design doc, not here. Link, do not duplicate.
- None reached this session — Phase 1 is incomplete.

**Touched**

- `CoreAPI` — **read only, nothing modified.** Branch as found: `CBS-4643-ofccp-getjobs-param-validation`, HEAD `fcf24ca5`. No CBS-4436 branch cut yet.
- Vault: `wiki/CBS-4436-v2-posting-analytics-wrap.md` (new, commit `9877714`), this file.

**Pipeline**

- Verdict: not run — Phase 1 is not finished, so there is no design doc to hand a Planner.
- `CoreAPI` has **no pinned test command** in [[protocols/coding-standards.md]] yet. The repo's own tracked `CLAUDE.md` gives `dotnet test`, with integration tests needing a local MSSQL container — unverified by actually running it. Pin it before the first pipeline run here.

**Blockers / Open questions**

- [ ] **NEXT — 2026-09-18: resume Phase 1 at Q1** in [[wiki/CBS-4436-v2-posting-analytics-wrap.md]]. Q1 = gate the decision on the per-channel wrap-rate query, or decide on existing evidence. Q2–Q9 are mapped there and unasked.
- **All three MSSQL MCP servers failed to connect on 2026-09-17** — `qa-64recs-mssql`, `uat-64recs-mssql`, `prod-64recs-mssql`, all `CONNECT_TIMEOUT` at 30s. A connection failure, not a missing capability. Worth retrying or checking the server config before the next session, because three open questions need `64recs67o` data:
  - the ticket's discovery question 1 (per-channel wrap rate);
  - which code path created the affected postings (e.g. `285573508`);
  - whether `POST /api/experiments/job/{jobId}/order` sees prod traffic.
- Found during recon, **not in the Jira ticket**: `OrderCommands.AddOrderItemAsync` writes `jobs_sites` rows directly, bypassing `PostingCreateCommands`, exposed live as `POST /api/experiments/job/{jobId}/order`. A third unwrapped writer. Tracked as Q7 in the design doc. Not yet raised with the reporter.
- Jira labels on this ticket include `MR_bypass`, `UAT_Bypass`, `noqarequired`. Noted, not acted on, not interpreted.

---

## 2026-09-17 (session 2)

**Did**

- **Resumed and completed Phase 1.** Q1–Q9 plus the testing and process questions are all decided.
  Design doc is now `status: decided`: [[wiki/CBS-4436-v2-posting-analytics-wrap.md]].
- Read `dbo.usp_CreateWrappedUrl` and `PM.usp_get_jobs_applyurl_not_wrapped` from the **DataGrip prod
  cache (snapshot 2026-07-08)** — MSSQL MCP still down. Both contradict parts of the ticket.
- Ran the **Athena ALB prod** datasource via Grafana to settle Q7 empirically — the first time that
  route has been used here. 200-day window on the prod core-api ALB.
- Established a **green `CoreAPI` test baseline** and pinned the command in
  [[protocols/coding-standards.md]] — the first row in that table.
- Wrote and syntax-checked the Q8 gate experiment runner (`cbs4436-experiment.ps1`). **Not executed.**

**Decisions**

- Decisions live in the design doc, not here. Link, do not duplicate.
- All 11 open questions closed there this session. The two that overturned the ticket's own premises:
  the proposed seam is unsafe, and there are three causes of a null `clickToApplyUrl`, not one.

**Touched**

- `CoreAPI` — **still read only, nothing modified.** Branch as found:
  `CBS-4643-ofccp-getjobs-param-validation`, HEAD `fcf24ca5`. No CBS-4436 branch cut yet.
  - Pre-existing uncommitted changes in that tree (**not mine**): `CoreAPI.TestFramework/CoreApiTestFramework.cs`
    (the documented `jtmssql` → `localhost` test-host edit) and
    `tests/CoreAPI.Tests.Integration/Helpers/SeederV1Extensions.cs`.
- Local docker: started the pre-existing `coreapi-test-sql` container. No container created or removed.
- Vault: this file, [[wiki/CBS-4436-v2-posting-analytics-wrap.md]],
  [[protocols/coding-standards.md]], [[wiki/CBS-4654-posting-read-timeouts.md]].

**Pipeline**

- Verdict: **not run.** Phase 1 only finished this session, and one gate is still open (see below).
- `CoreAPI` test command is now **pinned and verified green** — 367/367, 5m40s, 2026-09-17.
  ⚠️ Only green with the local MSSQL container up; without it the same command returns
  192 failed / 175 passed, **all environmental** (one `OneTimeSetUp: SqlException` bucket, zero real
  assertion failures). Precondition recorded in the protocol.
- Process decision: the ticket's `MR_bypass` / `UAT_Bypass` / `noqarequired` labels are **deliberately
  not being used** — the change outgrew what they were applied to. Reasoning in the design doc.

**Blockers / Open questions**

- [x] ~~NEXT — 2026-09-18: resume Phase 1 at Q1~~ — **done this session.** Phase 1 complete.
- [ ] **NEXT — gate before Phase 2 ships: run the `RegisterJobs` flip-count experiment.**
  **Blocked on credentials, not access.** Core-api requires an `X-API-KEY` header; the agent does not
  hold a prod key and did not go looking for one. **Neru runs `cbs4436-experiment.ps1` with the key in
  his own environment.** Read-only by default; prod write only under `-Execute`.
  - If most postings flip → the ticket's root cause holds and the chosen direction is right.
  - **If most stay null → the root cause is wrong and Q2 must be reopened before any Phase 2 work.**
- [ ] **Gate before Phase 2 ships: re-read both sprocs live.** Everything about `usp_CreateWrappedUrl`
  and `usp_get_jobs_applyurl_not_wrapped` came from a **2026-07-08 cache snapshot**, not a live read.
- MSSQL MCP servers still `CONNECT_TIMEOUT` on 2026-09-17 (`qa-`, `uat-`, `prod-64recs-mssql`).
  Datadog, GitLab, Jira (local), Lucidchart and AWS MCP also failed to connect. The **remote Atlassian
  connector and Grafana both worked** — prefer those while the local servers are down.
- **Q7 resolved, and it raises a new ticket.** `POST /api/experiments/job/{jobId}/order` is live,
  ungated and publicly routed but has **zero traffic in 200 days** of prod ALB logs. Left out of this
  ticket; **needs its own ticket to gate or remove the Experiments assembly.** Not yet filed.
- **Q9 needs a ticket too:** the ats-api `ThreadAbortException` wrap bug is split out.
  `atsapi-v6` is in the **Unassigned** list in [[README.md]] — confirm ownership before filing.
- Not yet raised with the reporter: the third-writer finding, and that the ticket's proposed seam is
  unsafe. Worth a comment on CBS-4436 before Phase 2 starts.
