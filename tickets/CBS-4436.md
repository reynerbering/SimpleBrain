---
type: ticket
ticket: CBS-4436
team: CBS
status: In Progress
source: jira
repos:
  - CoreAPI
updated: 2026-09-18
---

# CBS-4436 — Discovery: analytics click-to-apply hash missing for postings created via core-api V2 create path

- **Status:** Selected for Development (jira) — Investigation · High · reporter Shervin Ivari · assignee me
- **Design:** [[wiki/CBS-4436-v2-posting-analytics-wrap.md]]
- **Last touched:** 2026-09-17

## 2026-09-17

**Did**

- Pulled the ticket from Jira via the Atlassian MCP connector. Title, status, description verified — not reconstructed from `/raw`.
- Started Phase 1 grilling. **Paused after Q1 was asked; Q1 is unanswered and no decision was reached.**
- Read the V1 and V2 posting-create paths in `CoreAPI` to ground the interview in facts rather than the ticket's summary.
- Filed the design doc: [[wiki/CBS-4436-v2-posting-analytics-wrap.md]] (`status: in progress`). It carries the verified facts and the full Q1–Q9 decision tree.

**Decisions**

- Decisions live in the design doc, not here. Link, do not duplicate.
- None reached this session — Phase 1 is incomplete.

**Touched**

- `CoreAPI` — **read only, nothing modified.** Branch as found: `CBS-4643-ofccp-getjobs-param-validation`, HEAD `fcf24ca5`. No CBS-4436 branch cut yet.
- Vault: `wiki/CBS-4436-v2-posting-analytics-wrap.md` (new, commit `9877714`), this file.

**Pipeline**

- Verdict: not run — Phase 1 is not finished, so there is no design doc to hand a Planner.
- `CoreAPI` has **no pinned test command** in [[protocols/coding-standards.md]] yet. The repo's own tracked `CLAUDE.md` gives `dotnet test`, with integration tests needing a local MSSQL container — unverified by actually running it. Pin it before the first pipeline run here.

**Blockers / Open questions**

- [ ] **NEXT — 2026-09-18: resume Phase 1 at Q1** in [[wiki/CBS-4436-v2-posting-analytics-wrap.md]]. Q1 = gate the decision on the per-channel wrap-rate query, or decide on existing evidence. Q2–Q9 are mapped there and unasked.
- **All three MSSQL MCP servers failed to connect on 2026-09-17** — `qa-64recs-mssql`, `uat-64recs-mssql`, `prod-64recs-mssql`, all `CONNECT_TIMEOUT` at 30s. A connection failure, not a missing capability. Worth retrying or checking the server config before the next session, because three open questions need `64recs67o` data:
  - the ticket's discovery question 1 (per-channel wrap rate);
  - which code path created the affected postings (e.g. `285573508`);
  - whether `POST /api/experiments/job/{jobId}/order` sees prod traffic.
- Found during recon, **not in the Jira ticket**: `OrderCommands.AddOrderItemAsync` writes `jobs_sites` rows directly, bypassing `PostingCreateCommands`, exposed live as `POST /api/experiments/job/{jobId}/order`. A third unwrapped writer. Tracked as Q7 in the design doc. Not yet raised with the reporter.
- Jira labels on this ticket include `MR_bypass`, `UAT_Bypass`, `noqarequired`. Noted, not acted on, not interpreted.

---

## 2026-09-17 (session 2)

**Did**

- **Resumed and completed Phase 1.** Q1–Q9 plus the testing and process questions are all decided.
  Design doc is now `status: decided`: [[wiki/CBS-4436-v2-posting-analytics-wrap.md]].
- Read `dbo.usp_CreateWrappedUrl` and `PM.usp_get_jobs_applyurl_not_wrapped` from the **DataGrip prod
  cache (snapshot 2026-07-08)** — MSSQL MCP still down. Both contradict parts of the ticket.
- Ran the **Athena ALB prod** datasource via Grafana to settle Q7 empirically — the first time that
  route has been used here. 200-day window on the prod core-api ALB.
- Established a **green `CoreAPI` test baseline** and pinned the command in
  [[protocols/coding-standards.md]] — the first row in that table.
- Wrote and syntax-checked the Q8 gate experiment runner (`cbs4436-experiment.ps1`). **Not executed.**

**Decisions**

- Decisions live in the design doc, not here. Link, do not duplicate.
- All 11 open questions closed there this session. The two that overturned the ticket's own premises:
  the proposed seam is unsafe, and there are three causes of a null `clickToApplyUrl`, not one.

**Touched**

- `CoreAPI` — **still read only, nothing modified.** Branch as found:
  `CBS-4643-ofccp-getjobs-param-validation`, HEAD `fcf24ca5`. No CBS-4436 branch cut yet.
  - Pre-existing uncommitted changes in that tree (**not mine**): `CoreAPI.TestFramework/CoreApiTestFramework.cs`
    (the documented `jtmssql` → `localhost` test-host edit) and
    `tests/CoreAPI.Tests.Integration/Helpers/SeederV1Extensions.cs`.
- Local docker: started the pre-existing `coreapi-test-sql` container. No container created or removed.
- Vault: this file, [[wiki/CBS-4436-v2-posting-analytics-wrap.md]],
  [[protocols/coding-standards.md]], [[wiki/CBS-4654-posting-read-timeouts.md]].

**Pipeline**

- Verdict: **not run.** Phase 1 only finished this session, and one gate is still open (see below).
- `CoreAPI` test command is now **pinned and verified green** — 367/367, 5m40s, 2026-09-17.
  ⚠️ Only green with the local MSSQL container up; without it the same command returns
  192 failed / 175 passed, **all environmental** (one `OneTimeSetUp: SqlException` bucket, zero real
  assertion failures). Precondition recorded in the protocol.
- Process decision: the ticket's `MR_bypass` / `UAT_Bypass` / `noqarequired` labels are **deliberately
  not being used** — the change outgrew what they were applied to. Reasoning in the design doc.

**Blockers / Open questions**

- [x] ~~NEXT — 2026-09-18: resume Phase 1 at Q1~~ — **done this session.** Phase 1 complete.
- [ ] **NEXT — gate before Phase 2 ships: run the `RegisterJobs` flip-count experiment.**
  **Blocked on credentials, not access.** Core-api requires an `X-API-KEY` header; the agent does not
  hold a prod key and did not go looking for one. **Neru runs `cbs4436-experiment.ps1` with the key in
  his own environment.** Read-only by default; prod write only under `-Execute`.
  - If most postings flip → the ticket's root cause holds and the chosen direction is right.
  - **If most stay null → the root cause is wrong and Q2 must be reopened before any Phase 2 work.**
- [ ] **Gate before Phase 2 ships: re-read both sprocs live.** Everything about `usp_CreateWrappedUrl`
  and `usp_get_jobs_applyurl_not_wrapped` came from a **2026-07-08 cache snapshot**, not a live read.
- MSSQL MCP servers still `CONNECT_TIMEOUT` on 2026-09-17 (`qa-`, `uat-`, `prod-64recs-mssql`).
  Datadog, GitLab, Jira (local), Lucidchart and AWS MCP also failed to connect. The **remote Atlassian
  connector and Grafana both worked** — prefer those while the local servers are down.
- **Q7 resolved, and it raises a new ticket.** `POST /api/experiments/job/{jobId}/order` is live,
  ungated and publicly routed but has **zero traffic in 200 days** of prod ALB logs. Left out of this
  ticket; **needs its own ticket to gate or remove the Experiments assembly.** Not yet filed.
- **Q9 needs a ticket too:** the ats-api `ThreadAbortException` wrap bug is split out.
  `atsapi-v6` is in the **Unassigned** list in [[README.md]] — confirm ownership before filing.
- Not yet raised with the reporter: the third-writer finding, and that the ticket's proposed seam is
  unsafe. Worth a comment on CBS-4436 before Phase 2 starts.
- **Q3 may deserve a second look.** It chose seam (ii) partly on a self-deadlock argument that assumed
  a raw `SqlCommand` on its own connection. `Database.ExecuteSqlRawAsync` uses the context's own
  connection and transaction, so that argument does not apply and seam (i) is more viable than the
  write-up says. (ii) still looks right on the surviving arguments. Raised, unanswered.
- **The design doc's `OPEN QUESTION` markers were stale until corrected late on 2026-09-17** — the
  Decisions section had been written but the Open questions section still read "Q1 asked, unanswered".
  A self-contradicting doc that would also have halted the Planner. Now reduced to the genuinely open
  items: the two gates, the Q3 re-look, and telling the reporter.

---

## 2026-09-17 (session 3)

**Did**

- **Diagnosed and fixed the MCP connect failures.** Not VPN — the AWS Client VPN adapter is up and
  `jtawssql-lstnr` / `jtawssqlstag` / `jtawssqldev05` / `core-api` all resolve to 10.x. The cause is
  the npx cold start: `npm view @jobtarget/mssql-mcp-server` took **21s**, and `npx -y` on top of that
  blows the 30s MCP connect cap. Matches the known `@jobtarget/*` npx-timeout issue.
  - Added `--prefer-offline` to the 7 `@jobtarget` servers in `~/.claude.json`
    (`jira`, `gitlab`, `lucidchart`, `qa`/`uat`/`prod-64recs-mssql`, `datadog`).
  - Pre-warmed the npm cache for all 5 packages. Resolve now takes **2s**.
  - Backup: `~/.claude.json.bak-20260917-075027`. Edited as targeted text, **not** a JSON round-trip —
    the file contains keys differing only by case (`C:/JT Repositories/CoreAPI` vs `c:/...`) and a
    parse-and-rewrite would silently drop one.
  - ⚠️ **Requires a Claude Code restart to take effect.** Config changes cannot reconnect an MCP
    server mid-session.
- **Filed CBS-4716** — gate or remove the Experiments endpoint group. Investigation, Inbox, assigned
  to me. Checked for duplicates first; none. Carries the full 200-day zero-traffic evidence.
- **Commented on CBS-4436** for the reporter: the proposed seam is unsafe, there are three causes not
  one, and widening the backstop breaks the PostMaster contract. Comment `880451`.
- **Updated [[protocols/design-docs.md]]**: added the `grep -rn "OPEN QUESTION"` step before a doc may
  be marked `status: decided`. Prompted by today's own bug — the Decisions section was written while
  the markers stayed stale, so the doc contradicted itself and would have halted the Planner.

**Decisions**

- Decisions live in the design doc, not here. Nothing new decided this session.

**Touched**

- `~/.claude.json` (MCP config) + backup. `CoreAPI` untouched. Vault: this file,
  [[protocols/design-docs.md]].

**Pipeline**

- Verdict: not run. Gates still open.

**Blockers / Open questions**

- [ ] **The `RegisterJobs` experiment is STILL NOT RUN.** Checked `CORE_API_KEY`, `COREAPI_API_KEY`,
  `CORE_API_XAPIKEY`, `JT_API_KEY`, `X_API_KEY` across process/user/machine scopes — **none set**.
  Core-api requires `X-API-KEY` and the agent will not go hunting for prod credentials. `core-api.jobtarget.com`
  resolves to `10.70.124.22` and the VPN is up, so the only missing piece is the key.
  **Neru: set `$env:CORE_API_KEY` and run `cbs4436-experiment.ps1`.**
- [ ] **Restart Claude Code** to pick up the MCP fix, then re-read both sprocs live (GATE 2).
- [ ] **Q3 re-look still unanswered** — does `Database.ExecuteSqlRawAsync` change the seam decision?
- [ ] **ats-api ticket NOT filed** — deliberately. `atsapi-v6` is in the Unassigned list in
  [[README.md]]; filing it under CBS would invent a team assignment. Confirm ownership first.

---

## 2026-09-17 (session 4)

**Did**

- Ran three parallel investigation subagents (Q3 re-look, MCP diagnosis, `atsapi-v6` ownership).
  Every load-bearing claim was re-verified first-hand before being recorded here.
- **Q3 REOPENED.** The recorded reasoning does not survive review against HEAD `fcf24ca5`. Evidence and
  the per-argument breakdown are in [[wiki/CBS-4436-v2-posting-analytics-wrap.md]]; not duplicated here.
  Headline: **seam (ii) leaves the create response and the SNS `Create` payload carrying
  `clickToApplyUrl: null`** — the ticket's own symptom — because the `Posting` is materialised inside
  the transaction, before any post-commit wrap could run.
- **Cleared a stale `OPEN QUESTION`** — "the reporter has not been told" was already closed by comment
  `880451`. Verified live via JQL `comment ~` with a negative control and a description-only control,
  not taken from the session-3 log.
  - ⚠️ Anomaly, unexplained: CBS-4436's `updated` field reads `2026-09-16T19:54`, *earlier* than the
    2026-09-17 comment. The comment's text is directly searchable, so it stands as posted.
- **Design doc `status:` corrected `decided` → `in progress`.** Q3 being open makes `decided` false.
- **Resolved the Q9 filing blocker: `atsapi-v6` belongs to `PST` (Partnerships Team), not CBS or AS.**
  Independently verified: `project = PST AND text ~ "atsapi"` returns current ATS-API work
  (PST-7317/7314/7310/7309/7306), and commit-message keys in the repo since 2025-01-01 are PST-only
  (294 by leading-token count; zero CBS, zero AS).
  - ⚠️ The GitLab-group heuristic **misleads here**: the local remote is `oneclick/atsapi-v6`, but
    PST-6617/6618 describe the repo as `platform/core-api/ats-api` — the README's *CBS* pattern. The
    Jira-key evidence is what settles it. Live path unverified; `gitlab` MCP down.
- **Diagnosed the MCP failures properly. The session-3 theory was wrong about the binding constraint.**

**Decisions**

- Decisions live in the design doc, not here. **Nothing decided this session — Q3 went the other way,
  from decided back to open.**

**Touched**

- `CoreAPI` — **read only, nothing modified.** HEAD `fcf24ca5`, branch as found. Pre-existing
  uncommitted changes (not ours) left alone.
- `atsapi-v6` — read only.
- Vault: this file, [[wiki/CBS-4436-v2-posting-analytics-wrap.md]].
- No config file edited. No Jira issue created.

**Pipeline**

- Verdict: **not run, and must not run.** Q3 is open; Q4 and Q5 lean on it.

**Blockers / Open questions**

- [ ] **Q3 must be re-decided before Phase 2.** Neru's call. Seam (i) now looks stronger, but it hinges
  on a question only GATE 2 can answer: does a SQL error inside `usp_CreateWrappedUrl` doom the
  transaction so the following `CommitAsync` throws?
- [ ] **GATE 1 — `RegisterJobs` experiment still not run.** Unchanged. Needs `$env:CORE_API_KEY`.
- [ ] **GATE 2 — sprocs still unread live.** Now *more* important: it gates Q3, not just the sproc facts.
- [ ] **PST ticket for the ats-api `ThreadAbortException` wrap bug is drafted but NOT filed** — awaiting
  Neru's go-ahead. Bug is at `atsapi/Core/Common/Analytics.cs:34-36`; call sites
  `JobsController.cs:3668`, `ImporterController.cs:805` and `:1169`. `Analytics.PurgeJobShortURLsCache`
  (`Analytics.cs:159`) has the identical raw-`Thread` exposure.
- [ ] **[[protocols/tickets.md]] says "the two project keys in play are `CBS` and `AS`."** PST is
  demonstrably a third. Flagged, not edited — Neru's protocol to change.
- **Q5 porting hazard, new:** V1's targets query begins `USE [64recs67o];`
  (`CoreAPI.Endpoints.V1/Core/Services/AnalyticsService.cs:240`). Harmless on V1's private connection;
  a hazard once ported onto the pooled EF connection. Needs stripping or checking.
- **No in-repo precedent for `Database.ExecuteSqlRawAsync`** — zero hits repo-wide. The Q3 re-look's
  premise rests on behaviour with no usage and no test coverage here.

### MCP failures — root cause, corrected

Three independent causes. The session-3 `--prefer-offline` fix **landed and helped, but was not the
binding constraint**. Verified: 7 occurrences in `~/.claude.json`, 0 in the backup.

1. **Startup contention, not npx.** Every timing-out server initializes fine *alone* — `prod-64recs-mssql`
   2.5s, `grafana` 6.4s, `gitlab` 7.8s, `datadog` 8.4s, `jira` 6.6–12.0s, `mariadb-qa` 16.6s. Run just
   **two** concurrently and `jira` → **42.1s**, `mariadb-qa` → **30.6s**. All 16 servers launch at once
   against a fixed 30s-per-server cap, on a box at 82% CPU / 4.7GB free.
   - Explains what broke the old theory: `grafana` is not npx and was never patched, yet fails anyway.
   - The slowest server, `mariadb-qa`, runs under `uv` — nothing `--prefer-offline` touches.
   - `MCP_TIMEOUT` is **unset**, so the cap is the 30000ms default. Confirmed absent from
     `settings.json` and `settings.local.json`.
2. **Docker Desktop is not running** → the three `postgres-*` servers exit in ~491ms, which surfaces as
   `CONNECTION_CLOSED` rather than a timeout. Verified: `docker version` returns the npipe error.
3. **AWS SSO token expired** → `aws-mcp`'s `-32602`. `sts get-caller-identity --profile agent-toolkit`
   fails with "Token has expired and refresh failed".

**Network was ruled out again, with evidence.** VPN adapter up (10.202.0.149); `jtawssql-lstnr` →
10.6.130.29, `jtawssqlstag` → 10.5.150.9, `jtawssqldev05` → 10.4.141.185; TCP 1433 reachable on prod
and stag.

**Proposed fixes — none applied, all need Neru:**

- Set `MCP_TIMEOUT` (e.g. `120000`) in `~/.claude/settings.json` — no `env` block exists today, so it is
  an addition. ⚠️ Worth confirming against current Claude Code docs; only its absence was verified here,
  not that this build honours it. Restart required.
- Reduce the concurrent launch count — 3 MSSQL + 3 MariaDB + 3 Postgres environments start every session.
  ⚠️ Use `claude mcp` / `/mcp`, **never hand-edit `~/.claude.json`**: it holds keys differing only by
  case, and a parse-and-rewrite would silently collapse one.
- Start Docker Desktop. ⚠️ Then `docker start coreapi-test-sql` — never `docker run` a fresh one; no
  volume mount, the seeded DBs live in that container's layer.
- `aws sso login --profile agent-toolkit`.

---

## 2026-09-18

**Did**

- **Q3 deferred by Neru** — not re-decided until GATE 2 lands. The doom-transaction question is the
  deciding risk for seam (i) and is unanswerable without the live sproc DDL. **GATE 2 is now a hard
  prerequisite for Q3, and Q3 for Phase 2.** Recorded in [[wiki/CBS-4436-v2-posting-analytics-wrap.md]].
- **Added `MCP_TIMEOUT: 120000`** to `~/.claude/settings.json` (new `env` block; all 8 existing keys
  preserved, JSON validated before write, backup `settings.json.bak-20260918-003420`).
  ⚠️ **Requires a Claude Code restart**, and the fix is unproven — only the variable's prior absence was
  verified, not that this build honours it. If the timeouts persist after a restart, the next lever is
  cutting the concurrent server count, not raising the cap further.
- **Corrected [[protocols/tickets.md]]** — `PST` (Partnerships Team) added as a verified third Jira key,
  plus a warning that the GitLab-group heuristic misidentifies `atsapi-v6` as CBS.

**Decisions**

- Q3: **defer, do not decide.** Reasoning in the design doc.

**Touched**

- `~/.claude/settings.json` (+ backup). Vault: this file,
  [[wiki/CBS-4436-v2-posting-analytics-wrap.md]], [[protocols/tickets.md]].
- No repo touched. No Jira issue created.

**Blockers / Open questions**

- [x] ~~**Restart Claude Code**, then confirm whether `MCP_TIMEOUT` actually took.~~ — MCP came back
  later the same day via `/mcp` reconnects. **`MCP_TIMEOUT` is still unproven**: the reconnect was
  manual and per-server, so it is not evidence the env var worked. Leave the diagnosis open.
- [ ] Docker Desktop still down (blocks `postgres-*` **and** the `CoreAPI` integration suite) and the
  AWS SSO token is still expired (`aws-mcp`). Neither fixed — both need Neru.
- [ ] PST ticket still not filed — draft under review.

## 2026-09-18 (session 2) — GATE 2 landed

**Did**

- **GATE 2 complete.** MSSQL MCP came back (manual `/mcp` reconnects, all three envs). Both sprocs
  re-read live. Findings in [[wiki/CBS-4436-v2-posting-analytics-wrap.md]] → *GATE 2 — live re-read*.
  - **Snapshot fidelity: PASS.** `usp_CreateWrappedUrl` is byte-identical on prod, uat and qa, and
    matches the 2026-07-08 DataGrip cache the doc was built on. **No drift** — the whole "Sproc facts"
    section stands, including the three-causes finding behind Q2.
  - **Doom question: answered — NO, except deadlock.** Neither the sproc nor its nested
    `usp_oneclick_posting_custom_apply_url` sets `XACT_ABORT`, opens a transaction, uses `TRY…CATCH`,
    raises, or does an `INSERT … EXEC`. No trigger on `job_employanalytics`. Ordinary errors stay
    statement-level, so the transaction remains committable. 1205 is doom-class but **already dooms
    the create transaction today**, and the execution strategy already replays it.
  - **Rider for the Planner:** the "non-fatal wrap" cannot be a blanket catch-and-continue — it must
    re-throw on a dead transaction (1205 / `XACT_STATE() = -1`) or a retryable deadlock becomes a
    failed create.
  - **Side finding:** `PM.usp_get_jobs_applyurl_not_wrapped` catches everything and returns the error
    as a *result set*. A broken sweep looks identical to a clean one. Do not lean on the PostMaster
    backstop as a safety argument.

**Decisions**

- None taken. GATE 2 is evidence, not a decision — **Q3 is now decidable and is Neru's call.**

**Touched**

- Vault only: this file, [[wiki/CBS-4436-v2-posting-analytics-wrap.md]].
- Read-only against prod/uat/qa `64recs67o` (`sys.sql_modules`, `sys.triggers`, `sys.columns`,
  sproc definitions). **No writes.** `CoreAPI` read-only at HEAD `fcf24ca5`. No repo modified.

**Blockers / Open questions**

- [x] ~~**Q3 — Neru decides.**~~ **DECIDED same day: seam (i), in-transaction, before
  `GetPostingAsync`.** Reasoning in [[wiki/CBS-4436-v2-posting-analytics-wrap.md]] → Decisions → Q3.
- [ ] ⚠️ **NEW — Q4's reasoning died with Q3.** Q4 picked non-fatal and called it *forced* because
  post-commit the SNS had already fired. At seam (i) an in-transaction failure rolls back cleanly, so
  "fail the create" is a live option again. The conclusion may well still be right, but it is now a
  **choice nobody has made on those terms**. Not re-decided — Neru's call. **Blocks Phase 2.**
- [ ] **PST ticket — Neru said not yet.** Draft is line-verified and parked in the vault; nothing
  filed, no Jira issue created.
- [ ] **GATE 1** — still the only other Phase 2 blocker. Needs `$env:CORE_API_KEY`;
  `cbs4436-experiment.ps1`. Neru runs it.
- [x] ~~Jira status unverified~~ — re-verified live 2026-09-18 via the `jira` MCP server:
  **`In Progress`** (vault said `Selected for Development`; frontmatter corrected). Title matches.
  Jira `updated` is 2026-09-17T13:09 -0400.
- [ ] `datadog`, `grafana`, `lucidchart` still down; `aws-mcp` still `-32602` (SSO token).
  Docker Desktop still down → `CoreAPI` integration suite (192/367) still unrunnable.

## 2026-09-18 (session 3) — GATE 1 answered, both gates now clear

**Did**

- **GATE 1 closed.** Neru said core-api's `X-API-KEY` is not enforced in any environment and to call
  it directly. Confirmed: `GET /api/v2/posting/{id}` on prod returns **200 with no auth header**.
  The credential that blocked this gate for three sessions was never actually required.
- **The specified experiment is dead — 34 of the 35 affected postings have been remediated**, so the
  flip-test has a sample of one. Answered the underlying question from prod DB state instead.
  Full evidence in [[wiki/CBS-4436-v2-posting-analytics-wrap.md]] -> *GATE 1 - answered by DB state*.
  - **13/13 `is_pending_tracking = 1`** -> cause 2 (site opted out) eliminated.
  - **12/12 `apply_url_masking = 1`** -> cause 3 (nothing maskable) eliminated.
  - **12/12 analytics rows created 10-19 days *after* the posting**, in two manual batches
    (`07-17 14:56` x8, `07-20 13:15` x4) -> **cause 1 confirmed: the row was missing at create.**
  - **`285699513` is the proof case** - every gate green, *no* analytics row, **72 days on**. Not in
    `PM.campaign_posting` so the sweep's inner join excludes it, and now `expire < getdate()` so the
    sweep excludes it **permanently**. Created one minute before `285699605`, which *was* remediated
    by hand on 07-20. Nothing automatic has ever fixed these.
- **Verdict: the ticket's root cause HOLDS.** GATE 1's kill condition did not trigger. Q2's direction
  and Q3's seam (i) both stand.
- **Fixed a real bug in the runner** (`coding/scripts/cbs4436-experiment.ps1`): spider resolution used
  `count=200&page=1` and always returned **HTTP 400** - the endpoint rejects `count > 100`
  (`PostingController.cs:173`) and `page` is 0-based. It would have failed for Neru too.
- **Preserved the runner into the vault.** It existed only in a previous session's temp scratchpad and
  would have been lost to cleanup.

**Decisions**

- None taken by me. GATE 1 is evidence. **Q4 is now the only thing blocking Phase 2.**

**Touched**

- Vault: this file, [[wiki/CBS-4436-v2-posting-analytics-wrap.md]], `coding/scripts/` (new).
- Prod, **read-only**: 13 `GET /api/v2/posting/{id}`, 3 `GET /api/v2/job/{id}/posting`, and four
  `SELECT`s against `64recs67o`. **No writes. `RegisterJobs` was never called.**

**Blockers / Open questions**

- [ ] **Q4 - Neru decides. The only Phase 2 blocker left.** Its recorded reasoning died with the Q3
  re-decision; the conclusion may still be right but has not been made on seam (i)'s terms.
- [ ] **Neru to accept or reject GATE 1's substituted method.** It answers the gate's question, but it
  is not the experiment the gate specified. Not assuming acceptance.
- [ ] **No wrap-rate denominator anywhere.** We know this cohort's cause; we still do not know how
  often the gap occurs across channels.
- [ ] `285699513` is **still unwrapped in prod** and now unreachable by the sweep. It is expired, so
  the value of fixing it is low - but it is real, and it is a prod write, so **not touched**.
- [ ] PST ticket still parked unfiled at Neru's request.
- [ ] `datadog`, `grafana`, `lucidchart` down; `aws-mcp` `-32602` (SSO). Docker down -> CoreAPI
  integration suite (192/367) still unrunnable.

## 2026-09-18 (session 4) — Q4 decided, GATE 1 rejected and reopened

**Did**

- **Q4 DECIDED by Neru: non-fatal**, now on seam (i)'s terms rather than inherited from the void
  reasoning. Recorded in [[wiki/CBS-4436-v2-posting-analytics-wrap.md]].
- **Neru rejected the substituted GATE 1 method.** The DB-state evidence from session 3 stands as
  evidence and is not withdrawn, but it does not satisfy the gate. **GATE 1 reopened; it is now the
  sole Phase 2 blocker.**
- **Started rebuilding a current cause-1 cohort** so the *specified* flip-test can actually run. The
  original 35-posting cohort is spent (34 remediated, n=1). Subagent dispatched, scoped **read-only**
  against prod `64recs67o`, explicitly barred from any write and from calling `RegisterJobs`.
  Side benefit: this produces the **wrap-rate denominator** the ticket has never had.

**Decisions**

- Q4: **non-fatal.** A missing analytics hash should not stop a job going live, and GATE 1 evidence
  shows the row can be created later without harm. **The GATE 2 rider still binds** — re-throw on a
  dead transaction (`1205` / `XACT_STATE() = -1`); only committable failures may be swallowed.

**Touched**

- Vault only: this file, [[wiki/CBS-4436-v2-posting-analytics-wrap.md]]. No repo, no prod writes.

**Blockers / Open questions**

- [ ] **GATE 1 — the only Phase 2 blocker.** Specified flip-test must run against the rebuilt cohort.
  **Phase B is a prod write (`POST /internal/analytics/RegisterJobs`) and needs Neru to ask for it
  in the same breath.** Not to be inferred from "run the experiment".
- [ ] Docker Desktop down → `docker start coreapi-test-sql` needed before the `/ship` Tester stage,
  or 192/367 CoreAPI tests fail on connection errors rather than assertions.
- [ ] PST ticket still parked unfiled.
- [ ] `datadog`, `grafana`, `lucidchart` down; `aws-mcp` `-32602` (SSO).

## 2026-09-18 (session 5) — cohort rebuilt; PostMaster backstop found dead

**Did**

- **Rebuilt the GATE 1 cohort.** Subagent did the discovery (read-only, no writes, `RegisterJobs`
  never called); **every headline number re-verified independently before it was written here** —
  subagent numbers were accurate this time, bar normal production drift of 1-2 rows.
- **The wrap-rate denominator finally exists.** 442,461 live active postings, **21** with no active
  `job_employanalytics` row = **0.0047%**. Real, reproducible, 100% cause 1 — and tiny. Both halves
  of that belong in the ticket.
- **Corrected the cause model** (my own GATE 2 write-up was partly wrong). The sproc's `INSERT` is
  unconditional once its two gates pass, so unmaskable produces a row with NULL `tracking_url`, not a
  missing row. **"No row + `is_pending_tracking=1`" means the proc was never invoked — a non-call, not
  a no-op.** Cause 2 (14 of 37,452 sites opted out, **0** live postings on them) and cause 3
  (**0** live rows with null `tracking_url`) are both structurally empty.
- ⚠️ **Found that the PostMaster backstop has been dead since 2023-02-21.** `PM.campaign_posting` is
  frozen — max `created_date` 2023-02-21, max `posting_id` 205,036,529 against current ids of ~287M,
  **0** rows in the last month. The sweep's `INNER JOIN` therefore matches nothing and has returned an
  empty set for ~3.5 years. **And the sweep is `SELECT`-only — a report, never a repair.**
  **The ticket's "three backstops" premise is wrong: there are two, and one of those (ats-api) is
  itself the PST bug.**
- Subagent also claimed the MSSQL introspection tools are broken on this server.
  **Not reproducible** — `mssql_list_columns` and `mssql_get_stored_procedure_definition` both work
  here. Not carried into the vault.

**Decisions**

- None. All evidence. Decisions live in [[wiki/CBS-4436-v2-posting-analytics-wrap.md]].

**Touched**

- Vault only: this file, [[wiki/CBS-4436-v2-posting-analytics-wrap.md]].
- Prod `64recs67o`: `SELECT` only. **No writes. `RegisterJobs` never called.**

**Blockers / Open questions**

- [ ] **GATE 1 — the flip-test itself. Sole Phase 2 blocker.** Cohort is ready (21 postings).
  Firing it is a **prod write** and needs Neru to ask for that write explicitly. Re-run the cohort
  query immediately before firing; the count drifts.
- [ ] ⚠️ **The dead PostMaster sweep is its own defect and is NOT CBS-4436's to fix.** A scheduled job
  silently returning zero rows since 2023 should be raised separately. Not filed.
- [ ] Docker Desktop down → needed before the `/ship` Tester stage.
- [ ] PST ticket still parked unfiled.
