# AGENTS.md

This vault's behavioral instructions live in **[CLAUDE.md](CLAUDE.md)**. The folder
model and repo inventory live in **[README.md](README.md)**. Read both before acting.

`AGENTS.md` exists as the tool-agnostic entry point for agents that look for this
filename instead of `CLAUDE.md`. It is a pointer, not a second source of truth —
never duplicate rules here, because a divergent copy is worse than no copy.
